public class Greeter {
    /**
     * Returns a personalised greeting.
     *
     * Stage 2: return "Hello, <name>!" (trim whitespace from name)
     * Stage 3: if name is null, empty, or blank → return "Hello, stranger!"
     */
    public String greet(String name) {
        // TODO: Stage 2 — return "Hello, <name>!"
        // TODO: Stage 3 — handle null / empty / blank → "Hello, stranger!"
        return null;
    }
}
